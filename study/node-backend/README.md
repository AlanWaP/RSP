# Node Backend Study Copy

This directory keeps the original Node.js WebSocket backend for study and
comparison only. The active backend for the project is now the Go server in the
project root.

To run this study version separately:

```sh
npm install
npm start
```
